apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: pvviewer-role
  namespace: lab
rules:
  - apiGroups: [""]
    resources: ["persistentvolumes"]
    verbs: ["get, watch, list"]